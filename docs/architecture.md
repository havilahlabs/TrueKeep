# Truekeep First Release Architecture

- Local-first SQLite with typed domain models.
- Rules-based nudge engine (deterministic scoring).
- Expo Router app shell with 4 top-level tabs.
- One-time purchase model gates advanced features.
- Privacy lock uses device biometrics.
- Export creates local JSON backup file.
