# Widgets Contract

`services/widgetAdapter.ts` is the stable data contract.
Native iOS and Android widget implementations should consume the same payload keys:
- title
- subtitle
- deepLink
- nudgeId (optional)
